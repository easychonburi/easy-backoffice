{
  "firestore": { "rules": "firestore.rules" },
  "hosting": {
    "public": ".",
    "ignore": ["firebase.json", "firestore.rules", "functions/**", "scripts/**", ".git/**", ".github/**", "README.md"]
  }
}
