rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is through the authenticated, role-checked server API.
    match /{document=**} { allow read, write: if false; }
  }
}
