rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the server accesses Firestore, checking session + current staff permissions.
    match /{document=**} { allow read, write: if false; }
  }
}
